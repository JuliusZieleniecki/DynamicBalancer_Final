#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Copy this file to "credentials.h" and fill in your Wi-Fi details.
// credentials.h is gitignored and will never be committed.
#define DB_DEFAULT_WIFI_SSID "YOUR_WIFI_SSID"
#define DB_DEFAULT_WIFI_PASS "YOUR_WIFI_PASSWORD"

#endif // CREDENTIALS_H
